apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-redis.fullname" . }}
  labels:
    {{- include "retail-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: redis
      port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
  selector:
    {{- include "retail-redis.selectorLabels" . | nindent 4 }}
